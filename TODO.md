# 약사로 케어 — 할 일 목록

> 기준: 11차 종합 평가(2026-08-12) + 앱스토어 적합성 평가(2026-08-12) + **모바일 실렌더 UX 감사(2026-08-21)**
> 리포트: `_workspace/eval/final-evaluation.md` · `_workspace/eval_appstore/final-appstore-assessment.md` · `_workspace/eval/01b_ux-audit-mobile-2026-08-21.md`
> **현행화 실측: 2026-08-21.** `main` 클린 · 열린 PR 0 · 미커밋 0 · CI·프로덕션 스모크 초록 · Critical 0.
> 11차 High 6 중 **2건 해소**(PR #66) · 8/21 감사 High 4건 **전부 해소**(8d35c1c).
> 이 문서는 **남은 것**만 적는다 — 끝난 일의 이력은 `CLAUDE.md` 변경 이력에 있다.

---

## 🔴 지금 (규제 노출이 이미 열려 있다)

- [ ] **`/interactions` 페이지·`/api/interactions/check` 차단** — 라우트 삭제 또는 `notFound()`.
      **재실측 2026-08-21: 여전히 무가드다.** `NEXT_PUBLIC_SHOW_INTERACTIONS` 를 읽는 코드 `src/` 전체 0건,
      `src/app/interactions/` 에 `notFound()` 0건 → 링크만 없을 뿐 로그인 사용자면 URL 로 열린다.
      화면이 `병용금기`·`안전`·**"검출되지 않았습니다"**(음성 판정)를 면책 없이 표시 → 웰니스(비의료기기) 판정과
      Play 건강앱 선언에 동시에 걸린다. 반나절.
      ⚠️ **TWA 포장이 끝난 지금(8/21) 이 항목은 스토어 제출의 선결 조건이다** — 앱이 되는 순간 성격이 달라진다.
      ※ 플래그 방식을 유지하려면 **실제로 플래그를 읽게** 할 것.

---

## 🔒 사용자만 할 수 있는 일 (내가 대신 못 함)

- [ ] `supabase functions deploy ter-notify` — 배포본이 **version 10(8/11)** 이라 `passLen` 노출 코드가 **운영에 라이브**다.
      ▸ 누출은 로그가 아니라 **응답 본문**이다 — SMTP 실패 시 `creds: { userLen, userHasAt, passLen }` 를 502 바디로 돌려준다.
        `verify_jwt` 는 켜져 있지만 통과에 필요한 것이 **랜딩 HTML 에 공개된 anon 키**뿐이라 사실상 누구나 호출할 수 있다.
        커밋된 HEAD 소스는 이미 서버 로그로만 남기도록 고쳐져 있다.
      ▸ **보류 사유는 해소됐다(2026-08-21 확인)** — "`/ter` 워크스트림이 정리된 뒤" 라는 조건이었는데 그 워크스트림은
        **PR #69 로 머지됐고 061~063 도 운영 적용이 확인된다**(`list_migrations`: `20260815191901`/`191912`/`191925`).
        원문 유실 위험도 없다(MCP `get_edge_function` 으로 배포본 전문을 읽을 수 있고 `deploy_edge_function` 은 파일 내용을 그대로 받는다).
      함께: `index.ts:223` 의 `message: msg` 제거 · `encodeHeader()` CRLF 제거 · CI 에 `deno check supabase/functions/**/*.ts`
- [ ] **Play Console 확인 (리드타임 최장 — 다른 작업보다 먼저)** — health 앱에 **Organization 계정 + D-U-N-S** 가 실제로 요구되는지.
      사실이고 사업자등록이 없으면 **기술로 못 넘는다.** (TWA 포장은 이미 끝나 이제 행정만 남았다.)
- [ ] **낱알식별 v03 활용신청** — data.go.kr 에서 "의약품 낱알식별 정보" 활용신청(자동승인) 후
      `.env.local` 에 `MFDS_PILL_ID_KEY=` 추가. **2026-08-26 실측: 01/02 는 폐기, 03 이 현행인데 보유 키 4종 전부 403.**
      검색 API·UI·ETL 은 전부 준비돼 있고(067) 이 신청 하나가 실데이터 적재를 막고 있다 → 아래 🔎 섹션.
- [ ] GA4 속성의 **Google Signals / Ads 연동 상태** — 연동돼 있으면 Apple Tracking=Yes → ATT 필요 + 5.1.3(헬스 데이터 광고 이용 금지) 충돌
- [ ] 랜딩(`landing-deploy`) 재배포가 필요한 변경이 생기면 수동 — `vercel deploy --prod` (프로젝트 분리돼 있음)

> ~~사업자 정보 확정~~ — **법적 표기의무는 없는 것으로 확인(2026-08-12).**
> 전자상거래법 §10 의 수범자는 "사이버몰 운영자" 이고 사이버몰(§2조4호)·전자상거래(§2조1호)는 재화 **거래**를 전제한다.
> 무료·무판매 앱은 **§10 표시의무·§12 통신판매업 신고의무 모두 비적용**이고, Google Play 도 사업자등록번호는 **유료 앱에만** 요구한다.
> → `terms/page.tsx:8-11` 의 "임의로 채우지 않는다" 는 판단이 옳았다. **공란 유지가 정답이며 지어내 채우는 것이 허위표시 리스크다.**
> 필요해지는 시점: ① Play health 앱 Organization 계정이 요구될 때 ② **B2B 유료화 개시 시**.
> ※ 공개 법령·지침 1차 검토이며 법률 자문이 아니다.

---

## 🔎 의약품 정보 3종 잔여 (1단계 2026-08-26 완료 — 065·066·067)

> e약은요 DB 캐시 + DUR 노인주의·효능군중복 배지 + 낱알식별 검색은 코드·마이그레이션·QA 까지 끝났다.
> unit 107/107 · e2e 25스위트(신규 QA 3종 run-all 등록) · advisor 신규 WARN/ERROR 0. 배경은 `CLAUDE.md` 2026-08-26 행.

- [ ] **낱알 실데이터 적재** — 위 🔒 활용신청 후 `npm run etl:pill-id`(~250콜, 일 한도 내).
      스크립트가 첫 페이지 실키를 대조해 어긋나면 0행 적재로 중단한다(01→03 필드 개정 대비).
- [ ] **실검색 3종 확인** — 적재 후 흔한 약(타이레놀 등) 각인으로 검색해 정답이 후보에 드는지.
      `pill-search.tsx` 의 SHAPES/COLORS 어휘를 실데이터 distinct 와 대조해 조정.
- [ ] **낱알 이미지 Storage 복사** — 지금은 식약처 원본 URL 저장만(067 주석). 원본 링크 사망 대비는 별도 작업.
- [ ] e약은요 "없음" 네거티브 캐시(7일) — 커버리지 밖 약의 반복 외부호출이 문제될 때만.

---

## 📱 앱스토어 트랙

> **TWA 포장은 끝났다(2026-08-21, `27691e1`).** `twa/` 안드로이드 프로젝트 + `kr.co.yaksaro.care`(영구 고정) ·
> targetSdk **36**(2026-08-31 이후 빌드 요건 충족) · minSdk 23 · `enableNotifications: true` · 에뮬레이터 설치·실행·서명(v1·v2·v3) 검증 완료.
> `assetlinks.json` 은 **프로덕션 200 실측(2026-08-21)** — 주소창 제거 조건이 이미 배포돼 있다.
> 앱이 `care.yaksaro.co.kr` 을 로드하므로 **웹 배포 = 앱 갱신**(아이콘·이름·패키지가 바뀔 때만 재빌드).
> ⚠️ 서명 키(`twa/android.keystore`)는 미커밋 — **잃어버리면 같은 앱으로 두 번 다시 업데이트할 수 없다.**
>
> 결론은 그대로다: **Play 는 가능(코드 2~3일 + 행정 3주), iOS 는 지금 형태로 불가.**
> 그리고 **스토어가 지금 이 제품의 병목은 아니다** — 아래 "중기" 의 실측 수치를 먼저 볼 것.

### 공통 앱 코드
- [ ] **환자용 이메일+비번(또는 매직링크) 로그인 복원** — 심사 데모 계정 수단이 앱에 **없다**(`signup/page.tsx` 는 `redirect('/login')` 한 줄).
      구글은 해외 심사자 2FA 에 걸리고 카카오는 사실상 사용 불가 → 흔한 리젝. `pharmacy/login` 이 이미 `signInWithPassword` 를 쓰므로 패턴은 저장소 안에 있다
- [ ] **계정 삭제 요청 웹페이지** 신설(`/account-deletion`, 비로그인 접근) — 앱 내 삭제와 **별개로** Play Data safety **필수 필드**. 없으면 심사가 아니라 **제출이 안 된다**
- [ ] **접근권한 고지 화면**(정보통신망법 §22조의2 — 앱이 되는 순간 생기는 의무). 이 앱은 **필수 권한이 0** 이라 카메라·앨범·알림 전부 "선택" 으로 고지하면 끝
- [ ] manifest 보강 — `id`·`screenshots` 없음, maskable **192** 없음(512 만), `scope: "/"` 라 설치된 환자 앱에서 `/pharmacy/login` 이 열림(**iOS 4.3** 소지) → scope 축소 또는 배포 분리
- [ ] 아이콘 **1024×1024**(iOS, 알파 없음) + maskable-192 — `scripts/gen-pwa-icons.mjs` 의 `TARGETS` 에 2줄 추가
- [ ] 스토어 빌드에서 웹 전용 UI 숨기기 — 설치된 앱 안의 "앱 설치하세요" 는 웹 래퍼임을 자백하는 것 → `NEXT_PUBLIC_STORE_BUILD` 플래그
- [ ] 앱 내 **"의료기기가 아니므로 질병 유무를 판단할 수 없으며…"** 문구 — `src/` 전체에 "의료기기" 0건(랜딩에만 있다). 식약처 지침 Ⅴ-3 권고, 비용 0
- [ ] 로그인 후 처리방침 링크(Apple 5.1.1(i)) — 현재 `/login` 에만 있다. 10분
- [ ] 만 14세 확인 체크 1개 — 처리방침 제13조 선언과 구현 일치
- [ ] `/medications` 죽은 경로 → `redirect('/medications/add')` 1줄

### Google Play — 남은 것
- [ ] **`.aab` 미생성** — 현재 `twa/yaksaro-care.apk`(에뮬레이터 검증용)뿐이다. Play 업로드는 **App Bundle** 이라 번들 산출 필요
- [ ] **`POST_NOTIFICATIONS` 실기기 검증** — `twa-manifest.json` 의 `enableNotifications: true` 는 **선언일 뿐**이다.
      targetSdk 33+ 는 이 권한이 없으면 **알림이 예외 없이 조용히 안 뜬다** → `AndroidManifest.xml` 직접 확인 + Android 13+ 실기기로 알림 1건 수신.
      **048·PR#50·PR#51 과 같은 서명("등록은 됐는데 안 온다")**
- [ ] **`assetlinks.json` 지문 ↔ Play App Signing 인증서 SHA-256 재확인** — 지금 배포된 값은 로컬 업로드 키 지문(`7D:88:…:AB:01`)이다.
      Play 가 재서명하면 지문이 달라지고, 틀려도 크래시 없이 **주소창 남은 Custom Tab 으로 조용히 폴백**한다 → 출시 후 실기기 재확인
- [ ] 스토어 자산 — 스크린샷 4장+·특성그래픽 1024×500(현재 **0개**) · 설명문(의료기기 표현 금지 표로 자가 검수)
- [ ] Play 건강앱 선언 · Data safety 폼(복약·처방 텍스트=**Health info**, 처방전 원본 이미지는 즉시 파기라 ephemeral 예외로 "수집" 아님, **단골약국 열람(opt-in)은 "Shared" 로 볼 여지**)
- [ ] 개발자 계정 $25 + 전화번호 검증(2026-09 전면 의무화) · 개인 계정이면 **테스터 12명×14일** 후 프로덕션 신청

### iOS — 별도 의사결정에 부칠 것 (3~6주, 지금은 착수 비권장)
- [ ] Mac + Xcode 26 / Sign in with Apple(4.8 우회로 없음)
- [ ] **APNs 재구현** — `push_subscriptions` 토큰 종류 컬럼 + 마이그레이션 → `lib/push.ts` 2갈래 분기 → **cron 3종이 전부 두 경로**
- [ ] 네이티브 로컬 알림 + Face ID(4.2 방어 근거) · `PrivacyInfo.xcprivacy`(없으면 업로드 자체 거부)
- [ ] WKWebView 에서 죽는 3종 — `window.print()` 복약 리포트 무동작 / `<input capture>` 는 `WKUIDelegate` 없으면 무반응 / `getUserMedia` 는 `NSCameraUsageDescription` + 셸 권한
> **그 전까지 iOS 사용자에겐 Safari → 홈 화면 추가를 안내한다 — 역설적으로 그 경로에선 Web Push 가 살아 있고, 래핑하면 죽는다.**
> ⚠️ manifest 에 `prefer_related_applications: true` 를 넣으면 웹 설치 프롬프트가 억제된다. TWA 를 내더라도 **웹 설치 경로를 죽이지 말 것.**

---

## 🟠 11차 High 잔여 3건 (+ 위 `ter-notify` 배포 1건은 사용자 몫)

- [ ] **`db-gate` 를 실제로 돌게** — **재실측 2026-08-21: 여전히 실행 0회.** `ci.yml` 이 `if: github.event_name == 'workflow_dispatch'`
      인데 dispatch 실행 0건이고, 스케줄 실행에서는 `db-gate: skipped` 로 확인된다(같은 워크플로의 `quality`·`schema-gate` 는 success).
      → 14종(약사 RLS 누수 **27단언** 포함)이 아무도 안 보는 상태.
      `schema-gate` 가 증명한 해법 복제: **쓰기 불가능한 자격증명만 주고 "무엇이 0건인가" 만 단언하는 읽기 전용 잡**을 운영 대상 상시로 분리. 시드가 필요한 단언만 test DB
- [ ] **약사 회신 화면 진입점** — **재실측: `home-client.tsx:179` 하나뿐**(나머지 참조는 푸시 알림 URL 2곳과 주석).
      `isB2B` 일 때만 렌더되는데 연결 해제는 확인 없이 한 탭(`pharmacy-link.tsx:77-85`) → **되살린 화면에 도달할 방법이 사라진다.**
      설정에 "지난 요청·회신 보기" 상시 링크(5줄) + 해제 확인
- [ ] **약국 캘린더 월 이동** — **재실측: `pharmacy-calendar.tsx:20` 이 `monthGridDays(today)` 하나뿐, 이전/다음 버튼 0개.**
      저장(056 `due_date`)·API 는 임의 날짜를 이미 지원 → **막힌 건 화면 20줄.**
      약사가 요청한 맥락("저녁에 다음날 발주 메모")이 월말에 그대로 막힌다

---

## 🎨 모바일 UX 감사 잔여 (2026-08-21 감사 · High 4건은 당일 해소)

> 해소됨: H1 탭바 safe-area 붕괴 · H2 전달 화면 PRN 오표기 · H3 OCR 드롭존 무반응 · H4 페이지 도트 · M1 · M2 · M3 · M6.

- [ ] **M4. 캘린더 미래 날짜 대비 1.4~1.6:1**(기준 4.5) — 이번 달인데 "달력이 오늘에서 끝났다" 로 읽힌다
- [ ] **M5. 설정의 "건강정보 수집·이용 동의" 가 막다른 길** — ✗/✓ 표시만 있고 누를 수 없다. ✗ 를 본 사용자가 동의로 바꿀 경로가 그 화면에 없다
- [ ] **M7. 오늘복약 한 화면에 "지금 먹기" 3개** — 상단 배너와 아침 행이 같은 동작. 게다가 **지난 아침이 외곽선(약), 아직 안 온 저녁이 채움(강)** 이라 시각적 우선순위가 시급성과 반대다
- [ ] **L1. 홈에 약 추가 진입점 없음**(모바일은 약지갑 탭 안에만 있다) · **L2. 아주크게 모드 고아 줄바꿈**("전" 한 글자)
- [ ] **구조 (같은 부류의 재발을 막는 것)**
      - ~~`e2e/ux-safe-area-qa.mjs` 미등록~~ — **해소(2026-08-21)**: `ux-overlay-qa` 와 함께 `run-all.mjs` 공용 시드 그룹에 등록.
        (`ux-audit-render`·`ux-probe-*` 3종은 계측기라 미등록이 의도된 것)
      - **탭 타깃 최소치를 CI 가 강제하지 않는다** — 16px 링크가 살아남은 이유다. 44/48px 미만을 실렌더로 잡는 게이트
      - **고정 오버레이 정책 부재** — 탭바·도트·토스트가 각자 `bottom` 을 계산한다. `--yc-bottom-inset` 토큰 하나로 모으면 H1·H4 가 구조적으로 재발하지 않는다.
        ▸ 같은 공백에서 **72일짜리 결함**이 나왔다(2026-08-21 수정): 모달을 `fixed inset-0` 로만 두고 조상에 `will-change` 가 붙는 순간 갇힌다.
          지금은 `ux-overlay-qa` 가 잡지만, 근본 대책은 **모달을 body 로 포털**하는 것(조상이 무엇을 하든 무관해진다). 미착수
      - **전달 화면 데이터 계약 부재** — H2 는 `select` 한 줄 누락이 임상 정보를 왜곡한 사례다. 의사에게 나가는 필드 집합을 타입으로 고정
      - `ux-audit` 스킬의 파일 경로 10개가 **전부 부재**(현재는 `(main)` 라우트 그룹 + 병렬 슬롯 `@home`/`@wallet`/…) → 스킬 갱신

---

## 🩹 OCR 장애(2026-08-21) 잔여

- [ ] **실패 시 쿼터 환불 없음** — `consumeQuota` 가 파일 검증 뒤·파이프라인 앞에서 차감되므로 **서버 사정으로 503 이 나도 하루 한도(30회)가 준다.**
      감소 RPC 마이그레이션이 필요해 보류했고 `api/ocr/route.ts` 주석에 명시돼 있다. 사용자가 연속 실패를 겪은 날 한도까지 잃는 조합이 최악이다
- [ ] 원인 자체(일시적 아웃바운드 네트워크 장애)는 **재현 불가**로 종결. 다음 발생 시 `logger` 의 cause 체인
      (`fetch failed ← getaddrinfo ENOTFOUND … [ENOTFOUND]`)이 Sentry 에 그대로 오는지 확인할 것 — 그러라고 넣은 것이다

---

## 💰 B2B 유료화 (사용자 판단으로 보류 — "아직 이르다", 2026-08-11)

- [ ] 요금제·과금 수단 · 약국 셀프 가입 플로우 · 유료 조항·환불 규정
- [ ] **약사 비밀번호 재설정 경로 0건**(`resetPasswordForEmail`·`updateUser(` grep 0) — 약국 2곳만 넘어도 선형 부하가 되는 유일한 하드 블로커
- [ ] 착수 전 **`pharmacist-rls-qa` 를 자동 경로에** 올릴 것 — 타인 건강정보를 읽는 유일한 화면인데 그 게이트만 수동이다(위 `db-gate` 와 같은 항목)

---

## 🗓️ 중기 (제품 가치) — 운영 실측 2026-08-21

| 지표 | 값 | 비고 |
|---|---|---|
| 가입 계정 | **9** (최근 30일 **1**, 마지막 가입 **8/12**) | 상당수가 운영자 본인·카나리 |
| 마지막 복약 체크 | **2026-07-03** | **49일째 0** |
| 누적 복약 체크 | 36 | |
| 처방 행 / 복약 행 | 49 / 92 | |
| 약국 계정 | 2 | 외부 약국 0 |
| `/ter` 신청 | 1건 (8/11 접수, **10일째 `new`**) | 알림은 나갔고(`notified_at`) 회신은 없다 |
| 푸시 구독 | 5 | |

- [ ] **유입 실험 10명** — **감시 장치는 훌륭한데 볼 것이 없다.** 여정 A→B→H 완주율을 한 번 측정할 것.
      TWA 포장까지 끝난 지금, 다음 한 수는 스토어 행정이 아니라 **지인 파일럿**이라는 판단이 8/12보다 더 강해졌다
- [ ] **`/ter` 운영 동선** — 059 로 상태 컬럼은 생겼는데 **담는 사람도 기한도 없다.** 첫 신청이 10일째 `new` 이고 랜딩은 "분석을 마치는 대로" 라고 말한다.
      코드 문제가 아니라 약속을 지키는 문제다
- [ ] 신규 **환자** 카나리 스모크(`/home` 200 + 탭 5종) — 인증 스모크는 약사 전용이라 10차 최대 발견의 재발 방지가 절반이다
- [ ] `database.ts` 재생성 + 컬럼 드리프트 CI 게이트 — **059~063 이 누락**돼 있고 재생성 규약이 수기 패치로 퇴행했다(9차 근본 조력자의 재발 경로)
- [ ] 약사 대시보드 처방 변경 이력 · OCR 이름 폴백 유사도 · 프록시 role → JWT claim
- [ ] **"오늘" 규약 UTC→KST 통일** — 화면 3곳·`/api/meal-checks` 의 하루가 **UTC 자정(=KST 09:00)** 에 넘어간다
      (2026-08-22 리뷰에서 확인된 기존 결함): KST 00~09시의 복약 체크가 **전날 `check_date`** 로 남고,
      08:00~09:00 아침 체크는 어제 아침으로 기록된다. cron 은 KST 라 읽는 쪽과 이미 어긋나 있다.
      등록 당일 복용 시작 규칙은 화면 규약(UTC)에 **맞춰서** 넣었으므로 통일 시 `slotsApplicableToday` 호출부도 함께 이동할 것.
      데이터 마이그레이션(기존 check_date 재해석)이 걸려 있어 신중히 — 별도 작업으로

---

## 🧹 낮음 / 정리

- [ ] 약사 대시보드 **자식 조회 3곳** error 폐기(부모만 고쳐짐) · `end_expired_medications` 죽은 catch · `pharmacy/request` count fail-open
- [ ] **Sentry 소스맵** — `withSentryConfig` **0건 실측** · `SENTRY_AUTH_TOKEN` 없음. **DSN 이 켜진 지금 스택트레이스가 난독화된 채 온다**
- [ ] `@calendar` 오류를 빈 상태("기록 없음")로 표시 — **5회 연속 이월**(약사 화면은 고쳐졌다)
- [ ] `api/medications/bulk` `maxDuration` **0건 실측** + 타임아웃 8s/5s 통일 — **5회 연속 이월**
- [ ] 앱 문의처 `mailto:` 아님 · `store_id` 문자셋(운영 코드 `yc-jl2zm4` 에 `l`) — **인쇄물에 남는 값이라 다음 약국 발급 전이 가장 싸다**
- [ ] `.gitattributes` `* text=auto eol=lf` — **여전히 파일 부재 실측.** 10차 H1(CRLF 로 e2e 16종 침묵)의 근본 조건이 그대로다
- [ ] 레거시 테이블 `prescriptions`·`pharmacy_patients` DROP(0행·참조 0, 비가역이라 보류) · `_workspace/eval_*` 누적 정리
- [ ] 머지 완료된 원격 브랜치 정리 — `docs/policy-effective-date`·`fix/dur-member-scope` 는 스쿼시 머지 잔재다

---

## ✅ 상시 게이트 (자동화됨 — 깨졌을 때만 본다)

| 무엇 | 언제 | 실패 시 신호 | 2026-08-21 실측 |
|------|------|--------------|-----------------|
| `ci.yml` `quality`(tsc·lint·unit·build) | push·PR·하루 2회 | GitHub Actions | success (unit 80/80) |
| `ci.yml` `schema-gate`(운영 스키마 임베드 정합) | push·PR·하루 2회 | GitHub Actions | success |
| `smoke.yml`(익명 11 + 인증 6) | 프로덕션 배포 직후·하루 2회 | GitHub Actions | success |
| Sentry (`yaksaro` / `javascript-nextjs`) | 런타임 예외 | 이메일 | 동작 중 · 소스맵 미적용(위 🧹) |
| `notification_runs`(058) | cron 실행마다 | **행이 없으면 안 돈 것** | 8/16~20 **하루 6행**(끼니4+리필1+`ter_purge`1) 빠짐없이 · `failed` 0 |
| `db-gate`(RLS 누수 27단언 등 14종) | ❌ **실행 0회** | 없음 — 위 🟠 참조 | dispatch 0건 · 스케줄에선 `skipped` |

> ⚠️ 영수증은 **성공 경로만 실측됐다.** 8/12 이후 `failed` 가 0 이 아닌 적이 한 번도 없으므로
> **중단 경로(조회 실패·5xx)에 행이 남는지는 아직 눈으로 못 봤다.** 고친 것과 확인한 것은 다르다 — 첫 5xx 때 표를 볼 것.

로컬 전체 검증: `npm run test:unit`(80) · `npm run test:e2e:db`(14 스위트, 서버 불필요) ·
`npm run test:e2e:ui`(브라우저 5 스위트, dev 서버 필요) · `node e2e/ux-safe-area-qa.mjs`(미등록, 수동) · `npm run build`

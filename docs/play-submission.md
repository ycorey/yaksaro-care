# Google Play 제출 — 폼 답안지

> 실측 기준 2026-08-31. Play Console 에서 **그대로 옮겨 적을 수 있게** 답을 확정해 둔 문서다.
> 값이 바뀌면 여기부터 고치고, 화면·처리방침과 어긋나지 않는지 확인할 것.
> 코드 쪽 회귀는 `e2e/store-readiness-qa.mjs` 가 지킨다.

---

## 0. 먼저 끝나야 하는 것 (사람만 할 수 있음)

| 항목 | 왜 |
|---|---|
| **사업자 개발자 계정** 개설($25) + 신원 확인 | 개인 계정은 12명×14일 테스터 룰에 걸린다. Health 카테고리에 조직 계정이 요구된다는 보도가 있으나 **Google 공식 도움말에서는 확인되지 않았다** — 계정 개설 화면에서 실측하고 결과를 이 표에 적을 것 |
| **서명 키 백업**(`twa/android.keystore`) | 미커밋이다. **잃으면 같은 앱으로 두 번 다시 업데이트할 수 없다** |
| ~~`.aab` 산출~~ | ✅ **2026-09-01 완료** — `twa/yaksaro-care-release.aab` · 1,219,232 B · `jar verified.` |

---

## 1. App content — 필수 폼

### 건강 앱 선언 (Health apps declaration)
| 항목 | 답 |
|---|---|
| 카테고리 | **Medication management** — Google 정의: "복약 일정 관리, 조제 서비스, 복약 순응… 낱알 식별·복약 알림·처방 관리 포함". 이 앱 그대로다 |
| 규제 승인(FDA/CE 등) | **없음** → 비의료기기로 신고 |
| 근거 | 식약처 「의료기기와 개인용 건강관리(웰니스) 제품 판단기준」 — 데이터 표시·저장으로 만성질환자의 건강관리를 돕는 제품은 웰니스. 생체측정 데이터를 분석해 **치료 피드백**을 주면 의료기기인데, 이 앱은 그 판정을 하지 않는다 |

> ⚠️ 이 답을 지탱하는 것이 **음성 판정 0** 이다. "검출되지 않았습니다" 같은 문구가 화면에
> 하나라도 있으면 위 판단기준이 흔들린다. `/interactions` 를 삭제한 이유이고,
> `store-readiness-qa` 의 금칙 스캔이 지키는 선이다.

### 데이터 보안 (Data safety)
| 데이터 | 수집 | 공유 | 비고 |
|---|---|---|---|
| 이메일·이름 | 예 | 아니오 | 계정 |
| 복약 내역·처방 추출 텍스트 | 예 (**Health info**) | **예** | 단골약국 열람은 opt-in 이지만 **보수적으로 Shared 로 신고** — 제3자(약사)가 본다는 사실이 중요하다 |
| 처방전 원본 이미지 | **아니오** | — | 텍스트 추출 직후 즉시 파기 → **ephemeral 예외**. 저장하지 않는다 |
| 기기·접속 로그 | 예 | 아니오 | 호스팅·오류 진단 |

- 전송 중 암호화: **예**
- 사용자가 삭제 요청 가능: **예**
- **계정 삭제 요청 URL**: `https://care.yaksaro.co.kr/account-deletion` ← 없으면 **제출 자체가 안 된다**

### 그 밖의 App content
| 폼 | 답 |
|---|---|
| 개인정보처리방침 URL | `https://care.yaksaro.co.kr/privacy` (공개·PDF 아님 — 요건 충족) |
| 광고 포함 | 아니오 |
| 콘텐츠 등급(IARC) | 의약·치료 정보 **참조 있음**. ⚠️ Apple 에서는 이 답이 **규제 의료기기 선언 트리거**다 — iOS 라운드 때 반드시 기억할 것 |
| 타겟층 | 만 14세 이상. 어린이 대상 아님 |
| 정부 앱 / 금융 / 뉴스 / COVID | 전부 아니오 |
| App access (심사자 계정) | 이메일+비밀번호 계정을 발급해 적는다. 앱에서 **[이메일로 로그인]** 을 눌러 사용. 심사 메모에 "구글·카카오는 심사자 계정으로 통과가 어려워 이메일 입구를 제공한다" 를 적을 것 |

---

## 2. 스토어 등록정보

### 설명 첫 문단 — 비의료기기 고지 (필수)
Play 건강앱 정책은 규제 승인이 없는 앱에 이 고지를 **설명 첫 문단에** 요구한다.
없으면 업데이트가 리젝된다. 앱 안(설정·약 지갑)과 같은 문구를 쓴다.

> 약사로케어는 의료기기가 아닙니다. 질병의 진단·치료·예방에 사용할 수 없고,
> 등록된 정보로 질병 유무를 판단하지 않습니다. 판단이 필요하면 약사·의사와 상담해 주세요.

### 설명문 자가검수

⚠️ 단어 단위로 훑지 말 것. `진단·치료·예방` 은 **필수 고지문 자체가 쓰는 말**이다
("질병의 진단·치료·예방에 사용할 수 **없고**"). 부정문까지 걸러내면 요건을 스스로 지운다.
걸러야 하는 것은 **효능을 주장하거나 없다고 단언하는 문장**이다.

금지 — 효능 주장: 「~을 치료합니다 / 진단합니다 / 예방합니다 / 완치 / 개선 효과 / ~에 좋습니다」
금지 — 음성 판정: 「안전합니다·안전해요 / 이상 없습니다 / 문제 없습니다 / 괜찮습니다 /
　　　　　　　　　검출되지 않았습니다 / 부작용이 없습니다」
금지 — 행동 지시: 「복용을 중단하세요 / 처방을 바꾸세요 / 용량을 줄이세요」
허용 — 부정문 형태의 면책: 「의료기기가 아닙니다 / 진단·치료·예방에 사용할 수 없습니다 /
　　　　질병 유무를 판단하지 않습니다」

(같은 기준의 코드 가드는 `e2e/store-readiness-qa.mjs` 의 `BANNED` 정규식이다 — 조사·어미 변형까지 잡는다.)

### 자산 (현재 0개 — 미완)
- 스크린샷 4장 이상: 약 지갑 · 오늘 복약 · 처방전 촬영 · 약 정보
- 특성 그래픽 1024×500
- 앱 아이콘 512×512 → `public/icons/icon-512.png` (배포본 사용)
  ⚠️ `scripts/gen-pwa-icons.mjs` 로 재생성하지 말 것 — 배포본을 재현하지 못한다(스크립트 상단 경고)

---

## 3. 앱 안에 이미 들어간 것 (2026-08-31)

| 요건 | 위치 |
|---|---|
| 심사자용 이메일 로그인 | `/login` → [이메일로 로그인] |
| 계정 삭제(앱 내) | 설정 → 회원 탈퇴 |
| 계정 삭제 안내(비로그인) | `/account-deletion` |
| 접근권한 고지(정보통신망법 §22조의2) | `/permissions` — **필수 권한 0**, 카메라·사진·알림 전부 선택 |
| 비의료기기 고지 | 설정(상시) · 약 지갑 하단 |
| 처리방침·약관 링크(앱 안) | 설정 → 안내 |
| §23 민감정보 동의 **강제** | `/consent` 게이트 — `(main)`·`medications` 레이아웃이 미동의를 차단.
  로그인 화면 체크는 클라이언트라 서버 강제가 안 됐고, **기존 가입자는 `/login` 을 다시 지나지 않아**
  영원히 미동의로 남았다(실측 7명 중 6명). 이제 `consent_health` 가 실제로 앱을 막는다 |
| 만 14세 확인 | 로그인 [필수] 체크. **이메일 경로만 서버가 거절**하고 소셜은 클라이언트 가드다.
  연령을 기록하는 컬럼도 없다 — 폼에 "연령 확인을 서버가 강제한다" 고 적지 말 것 |

---

## 4. 남은 것

- [ ] 사업자 개발자 계정 + Health 카테고리 계정 요건 실측
- [ ] 서명 키 백업 — **아직이다.** `twa/android.keystore` 는 gitignore 되어 저장소에도 없다.
      잃으면 `kr.co.yaksaro.care` 를 같은 앱으로 두 번 다시 업데이트할 수 없다
- [x] **`.aab` 산출 — 2026-09-01 완료.** `twa/yaksaro-care-release.aab` · 1,219,232 B · `jarsigner -verify` → `jar verified.`
      경로: `./gradlew bundleRelease`(미서명 1,176,138 B) → `jarsigner -signedjar … yaksaro-care`
      ⚠️ **gradle 은 서명하지 않는다**(`app/build.gradle` 에 `signingConfig` 없음 — bubblewrap 이 빌드 후 별도로 한다).
         다시 빌드하면 또 미서명본이 나온다.
      ⚠️ 경고 `self-signed` · `PKIX path building failed` 는 **정상** — 업로드 키는 자체 서명이어야 하고 CA 체인이 없다.
      ✅ 인증서 지문을 `.aab` 에서 직접 뽑아 `assetlinks.json` 과 문자 단위 일치 확인:
         `7D:88:ED:DE:69:EE:8F:EB:C6:D2:9F:1A:24:7A:1E:9E:56:AA:61:AE:A5:9F:A8:72:FF:45:0E:DB:35:F2:AB:01`
- [ ] 내부 테스트 트랙 업로드
- [ ] **`versionCode` 증가 규칙** — `twa/app/build.gradle:60` 이 `versionCode 1` 고정이라
      **두 번째 업로드가 거부된다.** Bubblewrap 은 `twa-manifest.json` 의 `appVersionCode` 를
      쓰므로 거기서 올리고 재생성할 것(직접 편집하면 두 파일이 어긋난다)
- [x] ~~**마이그레이션 070·071 적용**(SQL Editor)~~ — **운영 적용 완료**(커밋 `d094f0f`).
      070: `interactions`·`ingredient_interactions` 원문을 service_role 전용으로.
      071: 약사 열람에 §23 동의를 AND 조건으로(그 전에는 **본인은 못 보는데 약사는 본다** — 실측 2명).
      **회귀 가드 신설**(2026-09-01): `e2e/rls-readonly-qa.mjs` + `ci.yml` 잡 `rls-gate` 가
      푸시·PR·하루 2회 운영 대상으로 실측한다. 첫 실행 **11/11 PASS**
      (대조군 `dur_single_flags` 9,028행이 살아 있는 상태에서 누수 단언 6개 전부 0행)
- [ ] 스크린샷·특성 그래픽
- [ ] **`POST_NOTIFICATIONS` 실기기 확인** — `AndroidManifest.xml:26` 선언은 있다.
      남은 건 Android 13+ 실기기에서 알림 1건 실수신. targetSdk 33+ 는 런타임 권한이
      없으면 **예외 없이 조용히 안 뜬다**(048·PR#50·PR#51 과 같은 서명)
- [ ] **출시 후** `assetlinks.json` 지문 재확인 — 위에서 대조한 `7D:88:…:AB:01` 은 *업로드 키* 지문이고,
      **Play App Signing 이 재서명하면 최종 앱의 지문은 달라진다.** 틀려도 크래시 없이
      **주소창 남은 Custom Tab 으로 조용히 폴백**해 눈치채기 어렵다.
      → Play Console → 설정 → 앱 무결성 → **앱 서명 키 인증서**의 SHA-256 을 확인해 다르면
        `public/.well-known/assetlinks.json` 배열에 **추가**할 것(교체 아님 — 둘 다 두면 로컬 빌드도 계속 동작) 후 재배포·실기기 확인
- [ ] 약사 대시보드 앱 내 노출 — **manifest scope 로는 못 뺀다**(아래)

### 왜 약사 화면을 앱에서 못 뺐는가
manifest `scope` 는 **단일 경로 접두사**인데 환자 화면이 루트 전역에 흩어져 있어
(`/home` `/wallet` `/today` `/settings` `/medications` …) `/pharmacy` 만 제외하는 접두사가 없다.
그리고 **웹 배포 = 앱 갱신**이라 앱에서 지우면 웹에서도 지워진다.
display-mode 로 막으면 PWA 를 설치한 **약사**가 대신 막힌다.
→ 현실적 대응은 심사 메모에 두 역할을 설명하는 것이다. 분리하려면 별도 서브도메인이 필요하다.
